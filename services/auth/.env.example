# AutoDrive — Auth service

# Port the service listens on
PORT=4001

# JWT signing secret — MUST match every other service that validates our tokens
# (cars + reviews). Use a long random string in production.
JWT_SECRET=autodrive-dev-secret-change-in-prod
