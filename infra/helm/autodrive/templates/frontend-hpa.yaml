{{- if and .Values.frontend.enabled .Values.frontend.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "autodrive.fullname" . }}-frontend
  labels:
    {{- include "autodrive.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "autodrive.fullname" . }}-frontend
  minReplicas: {{ .Values.frontend.hpa.min }}
  maxReplicas: {{ .Values.frontend.hpa.max }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.frontend.hpa.cpuAverage }}
{{- end }}
