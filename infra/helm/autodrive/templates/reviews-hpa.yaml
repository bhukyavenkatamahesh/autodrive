{{- if and .Values.reviews.enabled .Values.reviews.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "autodrive.fullname" . }}-reviews
  labels:
    {{- include "autodrive.labels" . | nindent 4 }}
    app.kubernetes.io/component: reviews
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "autodrive.fullname" . }}-reviews
  minReplicas: {{ .Values.reviews.hpa.min }}
  maxReplicas: {{ .Values.reviews.hpa.max }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.reviews.hpa.cpuAverage }}
{{- end }}
