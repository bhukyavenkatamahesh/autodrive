{{- if and .Values.auth.enabled .Values.auth.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "autodrive.fullname" . }}-auth
  labels:
    {{- include "autodrive.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "autodrive.fullname" . }}-auth
  minReplicas: {{ .Values.auth.hpa.min }}
  maxReplicas: {{ .Values.auth.hpa.max }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.auth.hpa.cpuAverage }}
{{- end }}
